# 毕业答辩讲解稿（12–15 分钟）——负载均衡式在线评测系统（冻梨OJ）

> 适用：**在线评测系统（OJ）方向**毕业答辩陈述。  
> 你可以把“【PPT】”当作翻页提示；把“【讲解】”当作逐字稿；把“【可选补充】”当作被问到再展开的内容。  

---

## 0. 开场与自我介绍（约 30–45 秒）【PPT 1：封面】

【讲解】  
各位老师好，我是___，我的毕业设计题目是《**负载均衡式在线评测系统（Load-Balanced Online Judge）**》。  
本项目面向算法学习与竞赛训练场景，核心目标是：在保证**判题正确性与安全性**的前提下，通过**多编译服务器负载均衡**提升并发处理能力，并提供完整的用户与内容生态，包括题库、题单训练、讨论社区、竞赛信息等模块。  
接下来我将从背景与目标、系统架构、关键流程与实现、安全与测试、总结与展望几个部分进行汇报。

---

## 1. 项目背景与问题定义（约 1–1.5 分钟）【PPT 2：背景/痛点】

【讲解】  
在线评测系统的典型流程是：用户提交代码 → 服务端编译运行 → 对多组测试用例判定结果 → 返回 AC/WA/TLE 等状态。  
在实际使用中会遇到三个典型问题：

1. **评测计算密集**：编译与运行占用 CPU、内存，单机容易成为瓶颈。  
2. **并发与稳定性**：高并发提交时，系统需要具备可扩展能力，同时要能处理编译机异常、超时等情况。  
3. **安全风险**：用户代码不可控，必须防止越权访问、资源耗尽、沙箱逃逸等安全问题。

因此我的项目选择以“**主服务器 + 编译服务器池**”的方式构建分布式 OJ，通过负载均衡提升吞吐，并在安全与工程化方面做完整落地。

---

## 2. 项目目标与范围（约 1 分钟）【PPT 3：目标/功能清单】

【讲解】  
本项目的目标分为三层：

**第一层：核心能力（必须）**  
- 在线评测：支持 **C++ / Java / Python** 的编译、运行与多测试用例评测。  
- 评测调度：主服务器将任务按负载分发到多个 `compile_server` 实例。  
- 数据持久化：使用 **MySQL** 存储用户、题目、提交记录等核心数据。  

**第二层：完整产品闭环（增强）**  
- 用户系统：注册/登录、会话管理（Cookie Session，24小时过期）、个人中心与统计。  
- 题库与题目管理：题目列表、题目详情、提交记录查询；管理员题目 CRUD 与分页。  
- 题单训练：题单创建、编辑、批量加题、拖拽排序、可见性控制。  
- 社区讨论：Markdown 帖子、全局评论、行内评论、图片上传、公式渲染。  

**第三层：可运营与可维护（工程化）**  
- 管理员后台：用户管理、邀请码注册、系统操作日志审计、统计图表。  
- 自动化测试：引入 **Playwright** 进行端到端测试与报告。  
- 部署与运维：Docker / Docker Compose 进行容器化部署。

---

## 3. 系统总体架构（约 2 分钟）【PPT 4：架构图】

【讲解】  
系统采用“**OJ 主服务器 + 编译服务器集群**”的分布式架构：

- **Client（浏览器）**：用户访问 Web 页面，提交代码，查看结果与内容。  
- **OJ 主服务器 `oj_server`（默认 8094）**：  
  - 负责 Web 页面与 API 接口  
  - 负责业务逻辑（认证、题目、评测分发、题单、讨论等）  
  - 汇总评测结果并写入数据库  
- **编译服务器池 `compile_server`（如 8081/8082/8083）**：  
  - 专门处理编译与运行  
  - 对每个测试用例执行、收集输出并返回结果  
- **MySQL 数据库**：存储题目、用户、提交、题单、讨论、竞赛等数据  
- **爬虫模块 `crawler`（可选）**：抓取 Codeforces/LeetCode 竞赛信息并入库

【讲解】  
这样设计的直接收益是：**评测能力可以横向扩展**。当并发提交增多时，只需要增加 `compile_server` 实例数量即可提升吞吐；主服务器负责调度与聚合，系统整体更稳定。

---

## 4. 后端分层与关键模块设计（约 2 分钟）【PPT 5：后端模块】

【讲解】  
在 `oj_server` 内部，我采用了相对清晰的分层：

1. **Control（控制层）**：处理 HTTP 请求，组织业务流程，例如认证校验、参数校验、调用 Model、触发评测分发等。  
2. **Model（数据层）**：封装 MySQL 访问，集中管理表结构与 SQL，降低业务层耦合。  
3. **View（视图层）**：基于 **CTemplate** 渲染 HTML 模板，用于页面展示。  
4. **LoadBalance（负载均衡模块）**：维护编译服务器在线列表与负载值，按“最小负载优先”进行选择，并支持故障转移。  
5. **Session（会话模块）**：Cookie-based Session，支持 24 小时过期，保障接口鉴权。

【讲解】  
编译服务器 `compile_server` 侧，我将流程拆成三个关键点：

- **Compiler**：统一封装 g++ / javac 等编译命令与产物管理。  
- **Runner**：对运行过程设置资源限制（CPU、内存等），并在沙箱机制下执行。  
- **CompileRun**：整体串联“生成临时文件 → 编译 → 多测试用例运行 → 对比输出 → 聚合结果”。

---

## 5. 核心流程：一次判题从提交到返回（约 2.5–3 分钟）【PPT 6：提交流程时序】

【讲解】  
这里我重点讲一次评测链路的端到端流程，也就是系统最核心的路径：

1. **前端提交**：用户在题目页选择语言并提交代码。  
2. **主服务器解析与校验**：`Control::Judge` 解析 JSON 请求，校验登录状态与输入合法性，从数据库读取该题的测试用例与资源限制（CPU、内存）。  
3. **负载均衡选机**：`LoadBalance` 遍历在线编译服务器，选择当前 `load` 最小的机器。  
4. **分发评测任务**：主服务器通过 HTTP 将代码、语言、用例输入、资源限制发送到目标 `compile_server`。  
5. **编译与运行**：编译服务器完成编译；随后对多个测试用例逐个运行，收集 stdout/stderr、耗时、内存，并将输出与期望结果对比，得到每个 Case 的通过情况。  
6. **结果聚合与返回**：编译服务器返回聚合 JSON（包含 case 列表与 summary）；主服务器写入 `submissions` 表，最后把结果返回前端页面展示。

【讲解】  
这一流程中我特别关注两个“系统可靠性”的点：

- **故障转移**：如果某台编译服务器请求失败，会被移出在线列表，主服务器尝试分发给下一台，避免单点故障导致整体不可用。  
- **超时与熔断**：对整体评测与单用例运行设置硬性资源限制，防止恶意/死循环代码拖垮系统。

---

## 6. 负载均衡策略与可扩展性（约 1–1.5 分钟）【PPT 7：负载均衡】

【讲解】  
本项目的负载均衡采用“**最小负载优先**”策略：  
每次分发前遍历所有在线编译服务器，选择 `load` 最小的一台执行任务；任务分发后对该机器负载计数增加。  

这种策略的优点是实现简单、效果直观，适合 OJ 这种“评测任务耗时差异较大”的场景，可以让任务尽量平均分布。  

同时系统支持**横向扩展**：通过配置文件增加编译服务器地址即可扩容；当并发压力增长时，不需要改动业务代码。

【可选补充】  
如果老师追问算法复杂度：该策略选择机器是 O(n)；在编译机数量不大时可接受，未来可通过缓存或更细粒度指标（如 CPU 利用率）进一步优化。

---

## 7. 数据库设计：核心表与关系（约 1.5 分钟）【PPT 8：数据库ER/表设计】

【讲解】  
数据库使用 MySQL 8.0+，字符集 utf8mb4。表设计围绕“题目—用户—提交—内容生态”展开，核心表包括：

- **题目表 `oj_questions`**：题目编号、标题、难度、CPU/内存限制、题面（Markdown）、测试用例 JSON（`tail`）等。  
- **用户表 `users`**：用户名、密码（SHA256）、邮箱、昵称、电话、头像、角色（普通/管理员）、状态（正常/封禁）等。  
- **提交表 `submissions`**：用户、题目、评测结果码、耗时、内存、提交内容与语言、提交时间等。  
- **讨论相关表**：`discussions`、`inline_comments`、`article_comments`，支持帖子与两类评论形态。  
- **题单相关表**：`training_lists` 与 `training_list_items`，实现题单元数据与题目关联，并用 `order_index` 支持排序。  
- **后台与运营**：`invitation_codes`（邀请码）、`operation_logs`（操作审计日志）。  
- **竞赛表 `contests`**：存储爬虫抓取的竞赛信息，并对（source, contest_id）做唯一约束。

【讲解】  
这样的数据模型能支撑从“做题提交”到“内容讨论”和“训练计划”的完整闭环，也便于后续做统计与运营分析。

---

## 8. API 设计与接口规范（约 1–1.5 分钟）【PPT 9：API 示例】

【讲解】  
系统整体采用 HTTP/JSON 接口风格，认证采用 **Cookie-based Session**。举几个典型接口：

- 认证：`POST /api/register`、`POST /api/login`、`GET /api/user`、`GET /api/logout`  
- 题库：`GET /api/problems?page=&page_size=`、`GET /api/question/{number}`  
- 评测：`POST /judge/{number}`（请求体包含 code 与 language）  
- 题单：`GET /api/training/list`、`POST /api/training/create`、`POST /api/training/reorder` 等  
- 讨论：`GET /api/discussions`、`POST /api/discussion`、内联/全局评论增删查  
- 管理后台：题目与用户管理、邀请码生成、操作日志与统计数据接口

【讲解】  
接口统一返回 JSON，其中 `status` 表示成功与否，失败原因放在 `reason` 字段，便于前端统一处理与提示。

---

## 9. 安全设计与风控（约 1–1.5 分钟）【PPT 10：安全设计】

【讲解】  
OJ 系统的安全性非常关键，我主要从三方面做设计：

1. **认证与权限**：  
   - 密码使用 **SHA256** 哈希存储；  
   - 登录后设置 `session_id` Cookie，`HttpOnly` 并设置 24 小时有效期；  
   - 管理员接口严格鉴权，关键操作写入 `operation_logs` 审计。  

2. **输入验证与错误处理**：  
   - 对用户名、密码、邮箱、分页参数等进行长度与格式校验，减少注入与异常输入；  
   - 错误信息对用户友好，不暴露内部实现细节。  

3. **代码执行沙箱与资源限制**：  
   - 编译服务器对运行过程进行 CPU、内存等限制，并采用降权运行与隔离策略；  
   - 对超时、异常退出等情况进行熔断，避免 DoS 风险。

【可选补充】  
如果老师问“为什么要分离编译服务器”：把风险从主业务服务器隔离出去，减少被攻击面，同时也利于水平扩展。

---

## 10. 前端与用户体验设计（约 1–1.5 分钟）【PPT 11：界面/交互】

【讲解】  
前端目标是让用户“像在本地 IDE 一样做题”，并保持一致、现代的体验：

- 视觉体系采用**深色主题**与 CSS 变量，确保一致性与可维护性，并预留浅色主题扩展空间。  
- 题目、题单、讨论等以卡片化布局呈现，支持响应式适配。  
- 编辑体验：集成代码编辑器与 Markdown 编辑器（用于讨论与题面），并支持 **MathJax** 渲染 LaTeX 公式。  
- 富文本安全：Markdown 渲染后会进行过滤（如使用 DOMPurify 的思路）以防 XSS。

【讲解】  
此外管理员后台提供数据可视化（如用户增长、提交统计等图表），用于运营与维护决策。

---

## 11. 测试与工程化：如何保证“能用、好用、可维护”（约 1–1.5 分钟）【PPT 12：测试/部署】

【讲解】  
为了保证系统质量，我做了两类工程化建设：

1. **端到端测试（E2E）**：使用 **Playwright** 覆盖注册登录、进入题目、提交代码、拿到结果等关键路径，并生成测试与质量报告。  
2. **容器化部署**：通过 Docker / Docker Compose 对 MySQL、OJ、编译服务器等组件进行编排部署；生产环境仅暴露必要端口，数据通过 Volume 持久化，便于快速部署与迁移。

---

## 12. 项目创新点与亮点总结（约 1 分钟）【PPT 13：创新点】

【讲解】  
最后我总结一下本项目的主要亮点：

1. **分布式评测 + 负载均衡**：主从分离，评测能力可横向扩展，并具备故障转移能力。  
2. **安全优先的执行设计**：资源限制、隔离与降权执行，降低恶意代码风险。  
3. **从“判题”到“平台”的完整闭环**：题库 + 题单训练 + 讨论社区 + 竞赛信息 + 管理后台，具备持续运营的产品形态。  
4. **工程化与可维护性**：文档齐全、接口规范统一，并通过 E2E 测试保障核心链路稳定。

---

## 13. 演示建议（可选，约 1 分钟）【PPT 14：Demo】

【讲解】  
如果现场允许演示，我建议按“最能体现价值”的顺序：

1. 登录后进入题库 → 打开题目详情；  
2. 粘贴一段正确代码 → 提交 → 展示逐用例结果与汇总；  
3. 打开题单列表 → 展示题单详情与排序；  
4. 打开讨论区 → 展示 Markdown/公式/评论能力；  
5. （如果条件允许）展示多编译服务器同时运行，说明扩展性与负载均衡。

---

## 14. 结尾与展望（约 30–45 秒）【PPT 15：总结】

【讲解】  
以上是我的答辩汇报。本项目围绕在线评测的核心挑战——**性能、稳定性与安全性**，通过“主服务器 + 编译服务器池”的架构实现了可扩展的评测能力，并完成了用户、题库、题单、讨论、管理后台等完整模块，具备平台化形态。  

后续的优化方向包括：更精细的负载指标（CPU/队列长度）、更强的沙箱隔离与审计、以及对更多语言/题型的扩展。  
我的汇报到此结束，谢谢各位老师，欢迎提问。

